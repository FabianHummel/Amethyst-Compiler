#var a = "nice";
#
#var x = [1, "string", [10, a, "a string inside an array inside an array! =)", true], false, "nice"];
#
#print x;


for (var x = 0; x < 10; x = x+1) {
    if (x > 5) {
        return;
    }
}